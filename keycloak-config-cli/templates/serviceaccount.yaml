{{- if .Values.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "keycloak-config-cli.fullname" . }}
  labels:
    {{- include "keycloak-config-cli.labels" . | nindent 4 }}
{{- end }}